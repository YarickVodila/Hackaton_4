import React from 'react';
import { Bar } from 'react-charts'

const BarChart = () => {
    return (
        <Bar
            height={400}
            width={600}
        />
    );
};

export default BarChart;